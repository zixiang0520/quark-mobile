# OpenList 配置示例 - 复制为 .env 并填入真实值
# 优先级: 环境变量 > config.yaml

# OpenList 连接信息
OL_BASE_URL=http://localhost:5244
OL_USERNAME=admin
OL_PASSWORD=your_openlist_password

# 挂载路径（可选，默认使用 config.yaml 中的值）
OL_MOUNT_QUARK=/quark
OL_MOUNT_MOBILE=/mobile
